name: Send Thank You Emails

on:
  schedule:
    - cron: ""
  workflow_dispatch:

jobs:
  send_emails:
    runs-on: ubuntu-latest

    steps:
      - name: Call Vercel API endpoint
        run: |
          curl -L -v -X GET https://danloski.com/api/send-thank-you-emails
