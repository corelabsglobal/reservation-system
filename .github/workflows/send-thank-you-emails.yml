name: Send Thank You Emails

on:
  schedule:
    - cron: "30 8 * * *"
  workflow_dispatch:

jobs:
  send_emails:
    runs-on: ubuntu-latest

    steps:
      - name: Call Vercel API endpoint
        run: |
          curl -L -v -X POST https://danloski.com/api/send-thank-you-emails
